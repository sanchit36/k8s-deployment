apiVersion: v1
kind: Service
metadata:
  name: api-server
spec:
  type: NodePort
  selector:
    app: api-server
  ports:
    - protocol: TCP
      port: 8000 # internal cluster port
      targetPort: 8000 # container port
      nodePort: 30081 # NodePort for macOS host access
